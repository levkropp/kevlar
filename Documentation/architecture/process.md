# Process & Thread Model

*This section will be expanded during Phase 1.*

Kevlar separates the concepts of Process (resource container) and Thread (execution context),
enabling proper Linux threading via `clone()` with shared resources.
