# The internals of `ssh://demo.kevlar.dev`

```
$ ./setup.sh
$ sudo yarn dev
```

```
$ gcloud compute images create kevlar-demo-vmx --source-disk=kevlar-demo --source-disk-zone us-west1-a --licenses "https://www.googleapis.com/compute/v1/projects/vm-options/global/licenses/enable-vmx"
```
